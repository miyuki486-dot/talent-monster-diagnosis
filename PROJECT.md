# こども起業家研究所 ～博士ちゃんラボ～｜診断ツール

## 目的

「その子らしさで稼ぐチカラを」を掲げる、こども起業家研究所 ～博士ちゃんラボ～に興味がある親子へ、子どもの性格と強みを前向きに伝え、「参加してみたい」と思える入口を作る。

## 合意済みの仕様

- 回答者: 小学1〜6年生の子どもを持つ親
- メールアドレスは診断前に求めず、診断結果ページで「保存したい・もっと詳しく知りたい」希望者だけが登録する
- 診断は星座入力1項目＋8問: 普段の性格5問、好きなもの3問
- 星座と各質問の選択肢を押すと、「次へ」を押さずに自動で次へ進む
- 判定比重: 性格50％、好きなもの30％、星座20％
- 独自の12タイプへ分類する
- 結果: モンスター、性格、強み、親の声かけ、マルシェで試せること
- 追加料金のかからない文章パーツ組み合わせ方式で、メイン＋サブモンスター、回答から見えた3つの特徴、個別の声かけ、最初の一歩を回答者ごとに変える
- 自由回答は判定点数には使わず、入力がある場合のみ「本人が教えてくれた姿」として個別結果へ反映する
- 公式LINEを主ボタン、申込みフォームを副ボタンにする
- 現在は回答や星座を外部送信・保存しない。メール送信時はメールアドレスと診断結果だけを送る
- 診断結果はスマホからPDFファイルとして直接保存でき、印刷時もA4・1枚に収まる
- 現在のテスト中は「博士ちゃんマルシェで試すなら」以降をCOMING SOON表示にして、メール・LINE・申込みを操作できないようにする（PDF保存・印刷は利用可能）
- 公式の親しみやすさと、オリジナルの冒険モンスター世界を組み合わせる

## ファイル

- `dist/index.html`: 診断本体
- `dist/settings.js`: LINE・申込みURL、モンスター画像の差し替え設定
- `dist/monster-roster.png`: 初期の仮モンスター12体
- `dist/monster-eggs-roster.png`: 卵から顔を出した孵化前の12体

## 名前の変更

`dist/settings.js` の先頭にある `projectTagline`、`projectName`、`projectNickname`、`activityName`、`diagnosisName` を書き換えると、トップページ・ブラウザ名・結果シートへ反映される。診断ツール名は `diagnosisName` だけで変更できる。

テスト用の表示切替も `dist/settings.js` から変更できる。

- `resultActionsComingSoon`: `true` でマルシェ案内以下を準備中・操作不可、`false` で通常表示

## メール送信と1枚資料

- メールアドレスは診断結果ページで希望者だけが入力し、ブラウザへ永続保存しない
- `dist/settings.js` の `resultEmailEndpoint` が空の間は、外部へ何も送信しない
- 本番の送信先を設定すると、本人の同意後、ボタン操作でメールアドレスと結果本文だけを送信する
- 結果画面の「詳しい診断を見る」から、PDFの直接保存またはA4・1ページでの印刷を選べる
- PDF保存はブラウザ内だけで生成し、iPhone／iPadでは共有メニューから「ファイルに保存」を選べる
- 詳細シートの個別文章はブラウザ内で組み立てるため、AI APIや文章生成の従量料金は発生しない
- メール送信を本番接続した場合は、個別の才能ミックス・特徴・声かけ・最初の一歩も送信データに含める

## 今後の回答データ記録

- みゆきの希望: 回答内容・星座・診断結果を記録し、今後の傾向分析や診断精度の改善に使えるようにする
- 現時点では未実装のため、外部への記録や送信は行わない
- 実装時は、分析データ（回答日時・星座・8問の回答・診断結果）と連絡先データ（メールアドレス・同意・匿名ID）を分けて管理する
- 子どもの氏名、学校名、生年月日など、診断に不要な個人情報は集めない

## モンスター図鑑

診断トップと結果画面から、12タイプの「孵化前／孵化後」を一覧で見比べられる。孵化後の個別画像を差し替えた場合も図鑑へ反映される。

## 公開前に必要なもの

- 公式LINEのURL
- 申込みフォームのURL
- 必要に応じて公式ロゴ・本番モンスター画像
- みゆきによる質問文、診断結果、見た目の確認

## 固定公開

- GitHubリポジトリ: `miyuki486-dot/talent-monster-diagnosis`
- 固定URL: https://miyuki486-dot.github.io/talent-monster-diagnosis/
- `main` ブランチへ変更を送ると、GitHub Actionsが `dist/` をGitHub Pagesへ自動公開する
- パソコンや一時公開プロセスを停止しても固定URLは利用できる
