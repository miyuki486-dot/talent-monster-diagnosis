# こども起業家研究所 ～博士ちゃんラボ～｜診断ツール

## 目的

「その子らしさで稼ぐチカラを」を掲げる、こども起業家研究所 ～博士ちゃんラボ～に興味がある親子へ、子どもの性格と強みを前向きに伝え、「参加してみたい」と思える入口を作る。

## 合意済みの仕様

- 回答者: 大人または子ども本人（子ども回答時は保護者確認）
- メールアドレスは取得せず、詳しい診断結果は公式LINE経由で受け取る
- 診断は星座入力1項目＋7問: 普段の性格4問、好きなもの3問
- 回答者選択後に「男の子／女の子／答えたくない・その他」を選び、タイプ判定ではなく表示素材の切替にだけ使う
- 「答えたくない・その他」はタイプ決定後に男女2つの卵から相棒を選び、その選択をWeb・D1・LINE詳細・PDFで維持する
- 星座と各質問の選択肢を押すと、「次へ」を押さずに自動で次へ進む
- 判定比重: 性格50％、好きなもの30％、星座20％
- 独自の12タイプへ分類する
- 結果: モンスター、性格、強み、親の声かけ、マルシェで試せること
- 追加料金のかからない文章パーツ組み合わせ方式で、メイン＋サブモンスター、回答から見えた3つの特徴、個別の声かけ、最初の一歩を回答者ごとに変える
- 自由回答は判定点数には使わず、入力がある場合のみ「本人が教えてくれた姿」として個別結果へ反映する
- 簡易結果の表示後、その人専用の受取コードを発行し、コード入りで公式LINEを開けるようにする
- 公式LINEでコードを送ると、本人専用の詳細結果URLをボタンで受け取れる
- 詳細結果URLでは、既存のA4・1ページシートをWeb表示する。PDF保存・印刷ボタンは調整完了まで一時非表示
- 受取コードを最初に送ったLINEアカウントへ紐づけ、別アカウントからの利用を防ぐ
- 子ども回答では、診断体験を邪魔しない1回の保護者確認を入れる
- 診断結果下部の旧COMING SOON枠は削除し、LINEでの詳細結果受取を独立して利用できるようにする
- 公式の親しみやすさと、オリジナルの冒険モンスター世界を組み合わせる

## ファイル

- `dist/index.html`: 診断本体
- `dist/settings.js`: LINE・API・ブランド設定
- `dist/assets/monsters/`: 正式モンスター96体と対応表（manifest.json）
- `tools/extract_kirari_monsters.py`: 正本PDFから96体を同じ規則で再抽出するスクリプト

## 名前の変更

`dist/settings.js` の先頭にある `projectTagline`、`projectName`、`projectNickname`、`activityName`、`diagnosisName` を書き換えると、トップページ・ブラウザ名・結果シートへ反映される。診断ツール名は `diagnosisName` だけで変更できる。

## LINE受取と1枚資料

- 診断完了時にCloudflare Workerへ回答を保存し、受取コードを発行する
- スマホの公式LINEボタンは、対象公式LINEのトークを受取コード入力済みで開く。PCは公式LINEのQR／プロフィールを表示する
- LINE Messaging APIのWebhookで受取コードを照合し、専用の詳細結果URLを返信する
- LINEから開いた詳細結果画面では、まずWeb上で詳細結果を閲覧できる
- PDF生成コードは保持するが、保存・印刷ボタンは調整完了まで一般利用者には表示しない
- 詳細シートの個別文章はブラウザ内で組み立てるため、AI APIや文章生成の従量料金は発生しない
- LINEからPDFファイルそのものを送らず、スマートフォンで開きやすい専用URLをボタンで送る

## 回答データ記録

- Cloudflare D1で、受取用データと長期分析データを別テーブルに分ける
- 受取コードと個別結果URLは診断から30日、自由回答を含む受取用データは90日後に自動削除する
- 長期分析データには、選択回答・星座・診断結果だけを保存し、受取コード・LINE識別値・自由回答を含めない
- LINEユーザーID・受取コード・個別結果URLは、そのまま保存せずハッシュ化する
- 子どもの氏名、学校名、生年月日など、診断に不要な個人情報は集めない

## 構成

- GitHub Pages: 診断画面、簡易結果、詳細結果画面、既存PDF生成
- Cloudflare Workers: 診断保存API、LINE Webhook、個別結果API、期限切れデータの削除
- Cloudflare D1: 受取用データ、匿名分析データ、Webhook重複防止、アクセス回数制御
- LINE Messaging API: 受取コードの受付と、詳細結果ボタンの返信

## 現在の状態

- 今回の新仕様はローカル実装と自動テストまで完了
- 旧版のCloudflare Worker / D1 / LINE Webhookは正常稼働中
- 新仕様のD1移行・Worker更新・GitHub Pages更新は未実施

## モンスター図鑑

診断トップと結果画面から男女それぞれ12タイプを見比べられる。卵はカラー、孵化後／ジュニア／大人はCSSのシルエットで表示し、元のカラー素材は保持する。

## 公開前に必要なもの

- 公式LINEのURL
- 申込みフォームのURL
- 必要に応じて公式ロゴ・本番モンスター画像
- みゆきによる質問文、診断結果、見た目の確認

## 固定公開

- GitHubリポジトリ: `miyuki486-dot/talent-monster-diagnosis`
- 固定URL: https://miyuki486-dot.github.io/talent-monster-diagnosis/
- `main` ブランチへ変更を送ると、GitHub Actionsが `dist/` をGitHub Pagesへ自動公開する
- パソコンや一時公開プロセスを停止しても固定URLは利用できる
